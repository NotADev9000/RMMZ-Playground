This will format all '/Data' files so they are no longer minified.
Only needed to see the changes made as this file is autorun on commit
=====================================
1. Run Powershell (Press Alt+D and type "powershell" in folder to open Powershell at that directory)
2. 'cd' into project root directory
3. Run 'node .\.git\hooks\pre-commit'